spring:
  application:
    name: univendas
  datasource:
    url: jdbc:postgresql://localhost:5432/univendas
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
logging:
  level:
    root: warn